# Superseded

Use **[`landing-zones/compose`](../../landing-zones/compose)**.

This blueprint has been superseded by the canonical Docker Compose landing zone, which includes the hardened internal network, Egress Gateway, and Docker socket proxy.
